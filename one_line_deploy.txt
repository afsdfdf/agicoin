apt update && apt upgrade -y && apt install -y git nodejs npm nginx && curl -fsSL https://deb.nodesource.com/setup_18.x | bash - && apt install -y nodejs && mkdir -p /var/www && cd /var/www && rm -rf agicoin && git clone https://github.com/afsdfdf/agicoin.git && cd agicoin && npm install && export CI=false && npm run build && echo 'server { listen 80; server_name _; root /var/www/agicoin/build; index index.html; location / { try_files $uri $uri/ /index.html; } location /static/ { expires 1y; add_header Cache-Control "public"; } }' > /etc/nginx/sites-available/agicoin && ln -sf /etc/nginx/sites-available/agicoin /etc/nginx/sites-enabled/ && rm -f /etc/nginx/sites-enabled/default && nginx -t && systemctl restart nginx && echo "AGICOIN 已成功部署在 http://$(curl -s ifconfig.me)" 